name: docs

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  docs:
    permissions:
        contents: write

    uses: liara-engine/.github/.github/workflows/reusable-docs.yml@v0
    with:
      repository: liara-core
      target: dev
